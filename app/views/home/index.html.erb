<h1>Home Page</h1>
<h2>Estas viendo una lista de productos en vitrina</h2>

<%= link_to 'Categorias', categories_path%>
<%= link_to 'Productos', products_path%>
<%= link_to 'Cart', cart_path%>

<% if user_signed_in? %>
<h1> <p>Usuario actual</p> <%=current_user.email%> </h1>
<% else %>
    <p>Usuario actual es una visita</p>
    <p>Hola bienvenido a nuestra tienda virtual donde podras conseguir productos electronicos,
     <br> si quieres acceder a todos las funcionesde la página puedes regitrarte aqui <%= link_to 'Registrase', new_user_registration_path%> y realizar tus compras de manera online</p>
<% end %>


<div> 
    <%= link_to "User Sign in", new_user_session_path %>
    <%= link_to 'sign up', new_user_registration_path%>
    <%= link_to 'sign out', destroy_user_session_path, method: :delete%>
</div>

<br>


<div class="row">
  <div class="col-md-8 offset-2">
    <div class="container">
      <div class="row">
        <% @products.each do |product| %>
          <div class="col-md-3">
            <div class="card">
              <img class="card-img-top" src="https://www.panafoto.com/media/catalog/product/cache/6517c62f5899ad6aa0ba23ceb3eeff97/1/3/135129.jpg" alt="<%= product.name%>"/>
              <div class="card-block">
                <h4 class="card-title"> <%= product.name %></h4>
                <div class="card-text">
                  <div class="badge badge-primary"><%= product.categories.name if product.category_ids %></div>
                  <div>$<%= product.price %></div>
                </div>
                <div class="row float-right">
                  <div class="col-md-12">
                    <%= form_for :cart, url: cart_path, method: :put do |f| %>
                      <%= f.hidden_field :product_id, value: product.id %>
                      <%= f.hidden_field :quantity, value: 1 %>
                      <%= f.submit "Add to cart" %>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>



